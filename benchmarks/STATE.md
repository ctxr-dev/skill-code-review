<!-- GENERATED by benchmarks/experiments.py. DO NOT EDIT BY HAND. -->
<!-- Regenerate with: uv run python benchmarks/experiments.py {state|history|leaderboard} --apply -->

# Benchmark STATE (YOU ARE HERE)

Generated: 2026-06-14T20:08:01+00:00

Current PR set: pr5 (rung 5 PRs)
Baseline tag: Baseline@pr5 LOCKED  (run_id: base-pr5, sha: c4e1e09)

| metric | value | 95% CI |
|---|---|---|
| F1 | 0.593 | [0.537, 0.652] |
| recall | 0.727 | [0.600, 0.900] |
| precision | 0.500 | n/a |
| fp/PR | 1.60 | n/a |
| $/review | 9.7715 | n/a |
| F1 stdev | 0.030 | (over 3 rounds) |

> $/review is a PROXY for relative lever comparison (GATE-5: candidate cost <= 1.25x baseline), NEVER billed spend. Under the flat claude -p subscription no per-review dollar is billed; the figure is a list-price imputation from one identical estimator. Systematic estimator bias cancels in the ratio ONLY when baseline and candidate share the same telemetry path (both live-usage, or both char-estimate): never mix a retroactive char-estimate baseline with live-usage candidates. See code_review/cost.py.

Last proven win: baseline (ramp, 2026-06-09T20:37:54+00:00)

## Dead-ends ledger
| lever | pr_set | why failed | retry at |
|---|---|---|---|
| C1-tier-demote-low-risk-leaves | pr5 | GATE-1 recall (delta CI lo -0.10 < -0.03) + GATE-2 fp/PR (1.60->1.87) + GATE-4 stability (0.054 > 0.05); genuine quality regression for a cost win | never |
| L3-trim-cheaper-output | pr5 | Cheap-first passed (llm_trim -26pct, ~262s vs 356s on 2 pilots, golden recall identical). But the N=3 5-PR no-regression gate FAILED on fp/PR: recall flat 0.7273=0.7273, fp/PR rose 1.60->1.80 (+0.20), F1 0.5926->0.5714 (-0.021); 5-gate inconclusive (underpowered n=5): gate_3_progress=F, gate_4_stability=F. Cheaper decode bought no recall and added FPs. See experiments row lever3-pr5 (sha 8e28022). | pr10 |
| M1-merge-rep-defect-framing | pr5 | No-op against the target gold: root-cause mismatch. golden-1 is missed at the ACTIVATION/ROUTING stage, not the merge/ranking stage the lever targets. Empirical product run of lever HEAD on discourse-1 (base 3f71fa15 merge-base, head ffbaf8c5): only 8 specialists picked at tree_descend/llm_trim, the defect-framing specialists (principle-least-astonishment, antipattern-magic-numbers-strings, qa-maintainability) were trimmed before running and lang-javascript produced 0 findings, so NO multi-member cluster forms at utilities.js:182 and the merge tiebreak never fires. The 4+ member cluster only existed in base-r2 (30 specialists picked); base-r3 also had no :182 cluster (also FN). The single surviving :182 finding (qa-sustainability-green-software, minor, single-member) stays non-primary -> golden-1 remains FN/minor-non-primary. Caught golds held (golden-0 optimized_image.rb:149, golden-2 optimized_image.rb:120 both pri=True): no recall regression, but no recall gain either. STAGE-1 predicate missed_gold_now_caught=false at first PR; stopped before N=3. Open recall path for golden-1 is upstream activation/routing coverage of the defect-framing specialists at utilities.js:182, not the semantic-merge representative. | never |
| R1-ranker-demote-meta-and-coverage-fp-class | pr5 | MIS-SCOPED, not underpowered. Cheap-first via scripts/rerank.py (ranker-only, ~1 call/PR), n=4 samples per arm to control ranker stochasticity, lever HEAD vs baseline origin/main on 2 pilots. PRIMARY-SET counts did not move beyond baseline noise: cal.com-14943 baseline {4,3,3,4} mean 3.5 vs lever {3,4,4,3} mean 3.5 (identical); discourse-1 baseline {4,4,4,6} mean 4.5 vs lever {4,4,4,4} mean 4.0, the only delta being one baseline d=6 sample (baseline itself hit 4 in 3/4 samples), so the apparent drop is inside baseline noise, not lever-attributable. TARGET metric did not move because the named class does not exist in the scored skill-prod-primary set: scanning all 5 pilots, ZERO genuine meta/process or pure-coverage findings are present. The FPs the judge actually flags are concrete correctness/security claims (cal.com: catch-all-retry-increment, Prisma-update-in-catch; discourse: decompression-bomb, ImageMagick-extension-check, resize-loop-no-cap), NOT the lever's class, so the prompt demotion rules have nothing to bite on. The dispatch.py fallback guard (load-bearing half) only fires for OMITTED ranker indices, but specialists supply parseable confidence on every primary finding here, so it is inert on the measured path (deterministically correct per new unit tests, never triggered on this corpus). RECALL HELD: every sample of both arms kept all golden-matching primaries primary (cal.com deleteMany-OR-clause + retryCount-stale; discourse downsize-duplicate + hardcoded-10MB); no golden TP demoted. Green gate fully passes (ruff clean, mypy clean, 231 pytest pass) including the new omitted-critical-not-primary tests. Per the gate_plan (if no FP is shed, STOP, the rule is mis-scoped not underpowered) and the lever's falsifiability clause (deterministic recall-safety means a null is a real null), STOPPED before any N=5 spend. retry_at_pr_set=NULL: a larger PR set cannot make a demotion rule bite on a class the primary set does not contain; this is structural, not power-limited. Nothing pushed; working tree restored clean. | never |
| V1-adversarial-finding-verification | pr5 | STRUCTURAL, same class as R1, not underpowered. READ-ONLY ground-truth audit of every scored skill-prod-primary false positive across all 5 pilots (cal.com-14943, discourse-1, grafana-80329, keycloak-32918, sentry-67876), N=3 baseline rounds (base-r1/r2/r3 judge verdicts on disk), each FP traced to source at PR HEAD in tmp/repos. Result: 14/14 deduped scored FPs are REAL-BUT-UNLABELED defects (genuinely-present mechanism in the shipped PR diff, just outside the Augment/Greptile-seeded Martian gold set per plan section 3), 0 genuinely-wrong, 0 ambiguous. Verified in code: cal.com unguarded await in catch (scheduleSMSReminders.ts:189-197) aborts the for-of batch, else-branch retryCount bump on SMS-lock undefined (178-187) + deleteMany gt:1 purge silently delete legit reminders; discourse downsize loop ignores OptimizedImage.downsize false return (uploads_controller.rb:65-69) and tempfile.size nil-deref after rescue nil (55+72); grafana 10m->1m ticker (cleanup.go:77) + t.Cleanup-after-insert; keycloak null-deref registerIDPInvalidation(:103/:366), stale searchKey merge (:239), HashSet ordering loss (:244/:254); sentry broad except swallow (integration.py:425-429), uncaught get_user_info raise_for_status (:434), one-path identity guard (:495/:481), hardcoded HMAC test sig (test_integration.py:423/426, recomputed and mismatched). A CONSERVATIVE refuter (presence-of-mechanism = keep) sheds ~0 of these; it could only cut fp/PR by demoting REAL findings, which violates GATE-1 (recall) and the conservative-refuter rule, so V1 cannot lift measured F1 at pr5 without demoting real defects. The precision metric at pr5 is partly gold-label COVERAGE, not skill error. No target population for the V1 lever in the scored set. retry_at_pr_set=NULL: a larger PR set adds more real-but-unlabeled defects, not a refutable-FP population; only improving ground-truth (gold coverage) or a different (COST) axis can move the needle. Productive axes now: COST (cost-capture infra, plan 5.6, then specialist tier-demote) and/or ramping PRs / improving ground-truth for an honest F1 read. | never |
| footgun-specialist-quality | pr5 | KEEP rule fails: a caught gold was LOST (sentry g2 KeyError-missing-sender-key 3of3 -> 2of3, displaced in r3 by the strengthened fail-open None framing). keycloak recursion g0 NOT recovered (mis-framed as N+1 in 3of3 rounds; leaf edit had zero empirical effect on that gold). Only sentry g0 (null fail-open) was newly caught, and only in r2/r3. F1 delta CI straddles 0 (GATE-3 fail), F1 stdev exploded 0.030 -> 0.108 (GATE-4 fail, bimodal: lone r2 outlier). Gate verdict inconclusive. | pr12 |
| keycloak-recursion-pin-via-high-specificity-keyword | pr5 | Recall gain did NOT materialize. KEEP RULE requires recall UP (gold caught strictly more often). Part 2 product runner on keycloak-32918 x3 (18/16/17 findings, non-faulted, pins_used 12/11/13): recursion gold caught 0/3, identical to 0/3 base-pr5 baseline (NOT strictly greater). The pin demonstrably FIXED routing (owning specialist runs every round) but the specialist emitted no matching finding; the only session.getById findings are N+1/perf-framed (the documented baseline FN class). caught_golds_held TRUE (keycloak alias gold 3/3, cal.com control both golds held). The bottleneck is the specialist finding-generation stage, NOT routing/tree-descend, so this pin lever cannot move recall on this corpus. | never |

## Next action
Start A2 (calibration): tune workers/finding-ranker.md, the cheapest proven win (ranker-only, low token cost, averaged over N rounds via rerank.py). Then apply the 5-gate predicate against this locked baseline. Alternative: ramp consideration, move to a larger PR rung for a more powered signal once a calibration win is proven.

Total recorded experiments: 5
